name: Publish NuGet Package 'PanKunik.CleanArchitecture.BuildingBlocks'

on:
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET 10
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: '10.0.x'

    - name: Cache NuGet packages
      uses: actions/cache@v3
      with:
        path: ~/.nuget/packages
        key: ${{ runner.os }}-nuget-${{ hashFiles('src/**/*.csproj') }}
        restore-keys: |
          ${{ runner.os }}-nuget-

    - name: Restore dependencies
      run: |
          VERSION=${GITHUB_REF#refs/tags/}
          echo "Building project for version $VERSION"
          dotnet restore PanKunik.CleanArchitecture.BuildingBlocks.sln
          dotnet build PanKunik.CleanArchitecture.BuildingBlocks.sln --configuration Release --no-restore 
          
    - name: Pack NuGet package
      run: |
          VERSION=${GITHUB_REF#refs/tags/}
          dotnet pack PanKunik.CleanArchitecture.BuildingBlocks.sln --configuration Release /p:PackageVersion=$VERSION -o ./bin/Release --no-build --no-restore

    - name: Publish to GitHub Packages
      run: |
        dotnet nuget push ./bin/Release/*.nupkg \
          --source https://nuget.pkg.github.com/pankunik/index.json \
          --api-key ${{ secrets.GHCR_PAT }}